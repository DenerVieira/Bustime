{
  "name": "sptrans-netlify-proxy",
  "version": "1.0.0",
  "description": "Proxy para API Olho Vivo no Netlify Functions",
  "scripts": {
    "start": "echo 'Run locally using Netlify CLI'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    
  },
  "engines": {
    "node": "18.x"
  }
}
